# Verify Backlog

> Runs whose auto-verify (vspec) phase was skipped or deferred, awaiting a
> later verify pass. Populated by `/start-analysis` (risk-based skip) and its
> resume scan (user-requested deferral). Newest on top.

## Backlog

| run_id | doc_root | reason | date | status |
|--------|----------|--------|------|--------|
