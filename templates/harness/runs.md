# Analysis Harness Runs

> One row per run, newest first. Fast rows require `fast_schema=3`, `artifact_class=fast-v3`, a
> current output-contract and Reviewer fingerprint, `diff_rate <= 0.10`, and
> `delivery_ready=true`. Schema 2 rows are `fast-v2-legacy`; earlier rows are `fast-legacy`. Full
> rows retain the nine-document quality contract.

## Runs

| run_id | feature | entry_type | mode | profile | fast_schema | artifact_class | output_contract | ui_risk | reviewer | diff_rate | delivery_ready | started | status | ended |
|---|---|---|---|---|---:|---|---|---|---|---:|---|---|---|---|
