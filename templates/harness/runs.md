# Analysis Harness Runs

> One row per run, newest first. Fast rows require `fast_schema=2`, `artifact_class=fast-v2`, a
> current Reviewer fingerprint, `diff_rate <= 0.10`, and `delivery_ready=true`. Earlier Fast rows
> are `fast-legacy` and never delivery-ready. Full rows retain the nine-document quality contract.

## Runs

| run_id | feature | entry_type | mode | profile | fast_schema | artifact_class | ui_risk | reviewer | diff_rate | delivery_ready | started | status | ended |
|---|---|---|---|---|---:|---|---|---|---:|---|---|---|---|
